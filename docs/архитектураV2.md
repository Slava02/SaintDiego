Schedule service 
+ создание и обновление timeslots 
GET /locations
POST /locations

GET /timeslots
POST /timeslots
GET /timeslots/{id}
PUT /timeslots/{id}
DELETE /timeslots/{id}
PATCH /timeslots/{id}/activate
PATCH /timeslots/{id}/archive

Event service 
+ получение списка событий по фильтрам 
+ обновление события 
+ добавление пользователя в событие 

PUT /events/{id}/participant
PUT /events/{id}
DELETE /events/{id}
GET /events/{id}
GET /events
?participant_id
?status
?location
?service_id
?from_date
?to_date
+ что-то для пагинации

Добавление пользователя в событие:
PUT /events/{id}/participant/{id}


Participants
+ получение списка событий доступных для участника 
+ блокировка участника с комментарием
+ получение списка записей участника
+ Логика ограничений по участникам

GET participant - список всех живых пользователей
POST participant - создание нового пользователя при записи на собсеседование
GET participant/{id}
PUT participant/{id}/block
PUT participant/{id}/unblock
PUT participant
GET participant/{id}/events - список достпуных для записи событий

В самом сервисе:
Проверка доступен ли event для пользователя

Service Settings 
+ Настройка ограничений по сервисам 
+ Настройка доступных для добавления  сервисов
+ Получение информации по сервисам 

GET /services
POST /services
GET /service/{id}


Volunteer service 
+ Создание пользователя 
+ дальше schedule для волонтёров еще нужно будет сделать 

POST volounteer
GET volounteer/{tg_id}
PUT volounteer/{tg_id}

Report service 
+ экспорт данных 

Auth service 
+ авторизация и аутентификация