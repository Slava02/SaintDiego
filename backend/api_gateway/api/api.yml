openapi: 3.0.0
info:
  title: Saint Egidio Admin API
  description: API для управления временными слотами, локациями и услугами в административной панели Saint Egidio
  version: 1.0.0
servers:
  - url: http://localhost:8080/v1
    description: Development server
security:
  - BearerAuth: []
paths:
  /locations:
    get:
      tags:
        - locations
      summary: Получение списка локаций
      responses:
        "200":
          description: Успешный ответ
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Location"
                maxItems: 100
        "400":
          description: Ошибка валидации
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Internal server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      tags:
        - locations
      summary: Создание новой локации
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateLocationRequest"
        required: true
      responses:
        "201":
          description: Локация успешно создана
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Location"
              example:
                id: 3
                name: Новый храм
                address: ул. Новая, 67
        "400":
          description: Ошибка валидации
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Internal server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /services:
    get:
      tags:
        - services
      summary: Получение списка услуг
      responses:
        "200":
          description: Успешный ответ
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ServiceType"
                maxItems: 100
        "400":
          description: Ошибка валидации
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Internal server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /services/{id}:
    get:
      tags:
        - services
      summary: Получение информации об услуге по идентификатору
      parameters:
        - name: id
          in: path
          required: true
          style: simple
          explode: false
          schema:
            type: integer
            format: int64
      responses:
        "200":
          description: Успешный ответ
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceType"
              example:
                id: 1
                name: Исповедь
        "400":
          description: Ошибка валидации
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Internal server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"

  /services/registrations:
    get:
      tags:
        - services
      summary: Получение списка услуг и настроек по ним, доступных для регистрации
      responses:
        "200":
          description: Успешный ответ
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ServiceRegistration"
                maxItems: 100
        "400":
          description: Ошибка валидации
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Internal server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /services/registrations/{id}:
    put:
      tags:
        - services
      summary: Изменение настроек для услуги, доступной для регистрации
      parameters:
        - name: id
          in: path
          required: true
          style: simple
          explode: false
          schema:
            type: integer
            format: int64
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateServiceTypeSettingsRequest"
        required: true
      responses:
        "201":
          description: Настройки успешно добавлены
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceTypeSettings"
        "400":
          description: Ошибка валидации
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Internal server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /timeSlots:
    get:
      tags:
        - timeSlots
      summary: Получение списка временных слотов
      description: Возвращает список временных слотов с возможностью фильтрации
      parameters:
        - name: status
          in: query
          style: form
          explode: true
          schema:
            type: string
            enum:
              - active
              - archived
        - name: startDate
          in: query
          style: form
          explode: true
          schema:
            type: string
            format: date-time
        - name: endDate
          in: query
          style: form
          explode: true
          schema:
            type: string
            format: date-time
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/TimeSlot"
                maxItems: 100
        "400":
          description: Ошибка валидации
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Internal server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      tags:
        - timeSlots
      summary: Создание нового временного слота
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateTimeSlotRequest"
        required: true
      responses:
        "201":
          description: Временной слот успешно создан
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TimeSlot"
        "400":
          description: Ошибка валидации
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Internal server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /timeSlots/{id}:
    get:
      tags:
        - timeSlots
      summary: Получение временного слота по идентификатору
      parameters:
        - name: id
          in: path
          required: true
          style: simple
          explode: false
          schema:
            type: integer
            format: int64
      responses:
        "200":
          description: Временной слот
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TimeSlot"
        "400":
          description: Ошибка валидации
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Internal server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    put:
      tags:
        - timeSlots
      summary: Обновление временного слота
      parameters:
        - name: id
          in: path
          required: true
          style: simple
          explode: false
          schema:
            type: integer
            format: int64
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TimeSlot"
        required: true
      responses:
        "200":
          description: Временной слот успешно обновлен
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TimeSlot"
        "400":
          description: Ошибка валидации
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Internal server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    delete:
      tags:
        - timeSlots
      summary: Удаление временного слота
      parameters:
        - name: id
          in: path
          required: true
          style: simple
          explode: false
          schema:
            type: integer
            format: int64
      responses:
        "200":
          description: Временной слот успешно удален
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
        "400":
          description: Ошибка валидации
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Internal server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /timeSlots/{id}/activate:
    patch:
      tags:
        - timeSlots
      summary: Активация временного слота
      parameters:
        - name: id
          in: path
          required: true
          style: simple
          explode: false
          schema:
            type: integer
            format: int64
          example: 1
      responses:
        "200":
          description: Временной слот успешно активирован
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TimeSlot"
        "400":
          description: Ошибка валидации
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Internal server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /timeSlots/{id}/archive:
    patch:
      tags:
        - timeSlots
      summary: Архивация временного слота
      parameters:
        - name: id
          in: path
          required: true
          style: simple
          explode: false
          schema:
            type: integer
            format: int64
      responses:
        "200":
          description: Временной слот успешно архивирован
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TimeSlot"
        "400":
          description: Ошибка валидации
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Internal server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /events:
    get:
      tags:
        - events
      summary: Получение списка событий
      responses:
        "200":
          description: Успешный ответ
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Event"
        "400":
          description: Ошибка валидации
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Internal server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
components:
  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
  schemas:
    Location:
      type: object
      properties:
        id:
          type: integer
          format: int64
        name:
          type: string
        address:
          type: string
      required:
        - id
        - name
      example:
        id: 1
        name: Главный храм
        address: ул. Примерная, 123
    Error:
      type: object
      properties:
        code:
          type: string
        message:
          type: string
        details:
          type: string
      required:
        - code
        - message
      example:
        code: VALIDATION_ERROR
        message: Ошибка валидации
        details: Поле 'name' обязательно для заполнения
    CreateLocationRequest:
      type: object
      properties:
        name:
          type: string
        address:
          type: string
      required:
        - name
        - address
      example:
        name: Новый храм
        address: ул. Новая, 67
    ServiceType:
      type: object
      properties:
        id:
          type: integer
          format: int64
        name:
          type: string
      required:
        - id
        - name
      example:
        id: 1
        name: Исповедь
    CreateServiceTypeSettingsRequest:
      type: object
      properties:
        periodDays:
          type: integer
          format: int32
      required:
        - periodDays
      example:
        periodDays: 30
    ServiceTypeSettings:
      type: object
      properties:
        id:
          type: integer
          format: int64
        serviceTypeId:
          type: integer
          format: int64
        periodDays:
          type: integer
          format: int32
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
      required:
        - serviceTypeId
        - periodDays
      example:
        id: 1
        serviceTypeId: 1
        periodDays: 30
        createdAt: "2024-03-20T09:00:00Z"
        updatedAt: "2024-03-20T09:00:00Z"
    TimeSlotService:
      type: object
      properties:
        id:
          type: integer
          format: int64
        serviceRegistrationId:
          type: integer
          format: int64
        capacity:
          type: integer
          format: int32
        bookingWindow:
          type: integer
          format: int32
        time:
          type: string
          format: date-time
      required:
        - serviceRegistrationId
        - capacity
        - bookingWindow
        - time
      example:
        serviceRegistrationId: 1
        capacity: 20
        bookingWindow: 30
        time: "2024-03-20T09:00:00Z"
    Recurrence:
      type: object
      properties:
        frequency:
          type: string
          enum:
            - daily
            - weekly
            - monthly
        interval:
          type: integer
          format: int32
          minimum: 1
        endType:
          type: string
          enum:
            - never
            - date
        endValue:
          type: string
          format: date-time
          nullable: true
      required:
        - frequency
        - interval
        - endType
      example:
        frequency: weekly
        interval: 1
        endType: date
        endValue: "2024-04-20T19:00:00Z"
    TimeSlot:
      type: object
      properties:
        id:
          type: integer
          format: int64
        title:
          type: string
        type:
          type: string
          enum:
            - single
            - recurring
        locationId:
          type: integer
          format: int64
        capacity:
          type: integer
          format: int32
        startDate:
          type: string
          format: date-time
        endDate:
          type: string
          format: date-time
        status:
          type: string
          enum:
            - active
            - archived
        services:
          type: array
          items:
            $ref: "#/components/schemas/TimeSlotService"
        recurrence:
          $ref: "#/components/schemas/Recurrence"
      required:
        - id
        - title
        - type
        - locationId
        - capacity
        - startDate
        - endDate
        - status
        - services
      example:
        id: 1
        title: Утренняя служба
        type: single
        locationId: 1
        capacity: 50
        startDate: "2024-03-20T09:00:00Z"
        endDate: "2024-03-20T10:00:00Z"
        status: active
        services:
          - timeSlotId: 1
            serviceTypeId: 1
            capacity: 20
            bookingWindow: 30
            time: "2024-03-20T09:00:00Z"
          - timeSlotId: 1
            serviceTypeId: 2
            capacity: 30
            bookingWindow: 30
            time: "2024-03-20T09:30:00Z"
    CreateTimeSlotRequest:
      type: object
      properties:
        title:
          type: string
        type:
          type: string
          enum:
            - single
            - recurring
        locationId:
          type: integer
          format: int64
        capacity:
          type: integer
          format: int32
        startDate:
          type: string
          format: date-time
          x-validate:
            - rule: startDate must be in the future
              condition: startDate > now
            - rule: startDate must be before endDate
              condition: startDate < endDate
        endDate:
          type: string
          format: date-time
          x-validate:
            - rule: endDate must be after startDate
              condition: endDate > startDate
        services:
          type: array
          items:
            $ref: "#/components/schemas/TimeSlotService"
        recurrence:
          $ref: "#/components/schemas/Recurrence"
      required:
        - title
        - type
        - locationId
        - capacity
        - startDate
        - endDate
        - services
      example:
        value:
          title: Тест повторение
          type: recurring
          locationId: 1
          capacity: 100
          startDate: "2025-03-20T18:00:00Z"
          endDate: "2025-03-20T19:00:00Z"
        services:
          - serviceRegistrationId: 1
            capacity: 40
            bookingWindow: 30
            time: "2025-03-20T18:00:00Z"
          - serviceRegistrationId: 2
            capacity: 60
            bookingWindow: 30
            time: "2025-03-20T18:30:00Z"
        recurrence:
          frequency: weekly
          interval: 1
          endType: date
          endValue: "2025-04-20T19:00:00Z"
    Event:
      type: object
      properties:
        id:
          type: integer
          format: int64
        timeSlotServiceId:
          type: integer
          format: int64
        capacity:
          type: integer
          format: int32
        datetime:
          type: string
          format: date-time
      required:
        - timeSlotServiceId
        - capacity
        - datetime
      example:
        id: 1
        timeSlotServiceId: 1
        capacity: 20
        datetime: "2024-03-20T09:00:00Z"
    ServiceRegistration:
      type: object
      properties:
        id:
          type: integer
          format: int64
        minPeriodDays:
          type: integer
          format: int32
        serviceTypeId:
          type: integer
          format: int64
        serviceTypeName:
          type: string
      required:
        - id
        - minPeriodDays
        - serviceTypeId
      example:
        id: 1
        minPeriodDays: 30
        serviceTypeId: 1
        serviceTypeName: Исповедь
